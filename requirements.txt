# Full local dev deps. For deployment use requirements-deploy.txt (slim).
google-genai
groq
python-dotenv
requests
fastapi
uvicorn[standard]
twilio
resend
pillow
playwright        # dev only: capture_screens.py (README screenshots)
streamlit         # legacy: the old app.py console (superseded by server.py + web/)
